<p id="notice"><%= notice %></p>

<h1>Refs</h1>

<%= link_to("self", "http://192.168.122.143:3000/refs", {})%>
<%= link_to("todos", "http://192.168.122.143:3000/todos", {})%>
<%= link_to("developers", "http://192.168.122.143:3000/developers", {})%>

<table>
  <thead>
    <tr>
      <th>ID: Task</th>
      <th>ID: Dvlpr</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @refs.each do |ref| %>
      <tr>
        <td><%= ref.todo.id %>: <%= ref.todo.taskname %></td>
        <td><%= ref.developer.id %>: <%= ref.developer.name %></td>
        <td><%= link_to 'Show', ref %></td>
        <td><%= link_to 'Edit', edit_ref_path(ref) %></td>
        <td><%= link_to 'Destroy', ref, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<div>
  <h2>Input form test</h2>
  <input id="password" name="password" type="password" />
  <%= text_area :page, :comtent %>
</div>

<br>

<%= link_to 'New Ref', new_ref_path %>
